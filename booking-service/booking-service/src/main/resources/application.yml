server:
  port: 8001
spring:
  application:
    name: booking-service
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password: simplepassword
  jpa:
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true
  security:
    user:
      name: admin
      password: admin